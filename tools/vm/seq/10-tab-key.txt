# **Tab, which did nothing at all here.**
#
# The character was filtered out by a `>= ' '` test in `WM_CHAR` -- 0x09 is
# below it. The layout half had always worked (`rich_next_tab_stop`, and
# paragraph tab stops are honoured), so only the character was missing.
#
# **Sam measured all three possibilities and riched20 does the simplest:**
# Tab always inserts an `09` and never indents -- not at a paragraph start,
# not on a bulleted paragraph, not on an indented one, with `wNumbering`
# staying 1 and `dxStartIndent` staying 720 while the tab goes into the text.
# **Shift+Tab inserts one too; it is not an outdent.** Checked against WordPad
# itself and not only a bare control, Tab being exactly the key an MFC frame
# might have eaten.
#
# The dump tells the three apart without anybody choosing: a `09` in `text`
# says it inserts, `sel` moving alone would say it is a position, and
# `para ind` moving would say it indents.
type:97:0,type:108:0,type:112:0,type:104:0,type:97:0,type:32:0,type:98:0,type:101:0,type:116:0,type:97:0,tab:0:0,type:103:0,type:97:0,type:109:0,type:109:0,type:97:0
